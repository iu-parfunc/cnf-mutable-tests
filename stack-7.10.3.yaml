# This is used to do a FAKE run against a stock GHC.

resolver: lts-5.9

packages:
- '.'
- deps/compact-fake/
- deps/compact-indexed/

extra-deps: []

flags: {}

extra-package-dbs: []

system-ghc: false

local-bin-path: ./bin

